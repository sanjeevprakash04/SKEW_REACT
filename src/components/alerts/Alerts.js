import { Box, Typography } from '@mui/material';

function AlertsPage() {

    return(
        <Box mt={0.5} height="calc(100vh - 126px)">
            <Typography>
                Hello Alerts
            </Typography>
        </Box>
    );
};

export default AlertsPage;